# Maintenance Scripts

This directory contains one-off or maintenance utility scripts that were previously placed in the repository root.

These scripts are not part of the runtime application bundle and are kept here to keep the root directory clean.
